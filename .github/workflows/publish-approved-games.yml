name: Legacy - publicación Firebase Storage desactivada

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Información
        run: |
          echo "El publicador antiguo basado en Firebase Storage está desactivado porque TecnoMath usa Firebase Spark."
          echo "Los nuevos juegos se publican mediante GitHub Issues + etiqueta approved + GitHub Actions."
