name: Legacy - publicador anterior desactivado

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Información
        run: |
          echo "Este workflow histórico está desactivado."
          echo "NO publica juegos y NO utiliza Firebase Storage ni GitHub Issues."
          echo "La publicación vigente usa .github/workflows/process-game-queue.yml."
          echo "Flujo actual: gameSubmissions -> publicGameQueue -> Firebase Function -> game-approved -> GitHub Actions -> games/."