(function(){const KEY='tecnomath_progress_v1';const defaults={xp:0,coins:0,level:1,streak:0,lastDay:null,games:0,correct:0,wrong:0,topics:{},achievements:[]};function read(){try{return {...defaults,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return {...defaults}}}function save(d){localStorage.setItem(KEY,JSON.stringify(d));window.dispatchEvent(new CustomEvent('tecnomath:progress',{detail:d}));return d}function levelFor(xp){return Math.floor(Math.sqrt(xp/100))+1}function day(){return new Date().toISOString().slice(0,10)}function record(r={}){let d=read(),today=day();if(d.lastDay!==today){d.streak=d.lastDay?d.streak+1:1;d.lastDay=today}const correct=Number(r.correct||0),wrong=Number(r.wrong||0),bonus=Math.max(0,Number(r.xp||correct*25-wrong*2));d.xp+=bonus;d.coins+=Math.max(0,Number(r.coins||correct*5));d.games+=Number(r.games||0);d.correct+=correct;d.wrong+=wrong;d.level=levelFor(d.xp);if(r.topic){let t=d.topics[r.topic]||{correct:0,wrong:0};t.correct+=correct;t.wrong+=wrong;d.topics[r.topic]=t}const acc=d.correct+d.wrong?Math.round(d.correct/(d.correct+d.wrong)*100):0;[['first_win',d.correct>=1],['century',d.correct>=100],['streak3',d.streak>=3],['master',d.level>=10],['precision',acc>=90&&d.correct>=20]].forEach(([id,ok])=>{if(ok&&!d.achievements.includes(id))d.achievements.push(id)});return save(d)}function reset(){return save({...defaults})}window.TecnoMathProgress={read,save,record,reset,levelFor};})();